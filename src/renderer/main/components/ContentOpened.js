import React from 'react'
import {
  Text,
} from '../../_shared/components'


const ContentOpened = () => (
  <div>
    <Text>Wait a while before new window appears then scroll in the window where the game is located.</Text>
  </div>
)

export default ContentOpened
